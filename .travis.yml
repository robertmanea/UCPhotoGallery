language: objective-c

install:
  - gem install xcpretty --no-rdoc --no-ri --no-document --quiet

osx_image: xcode7

xcode_project: UCPhotoGallery.xcodeproj
xcode_scheme: UCPhotoGalleryExample
xcode_sdk:
  - iphonesimulator9.0

notifications:
  email: false
